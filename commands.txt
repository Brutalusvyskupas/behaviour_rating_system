docker build --tag behaviour-rating-system .
docker run --publish 8000:8000 behaviour-rating-system



docker-compose build
docker-compose run --rm app django-admin startproject core .
docker-compose up (starts server)

docker exec -it django_container /bin/bash (can run normal python, django commands)
docker-compose run web python3 manage.py migrate

